{
  "mcpServers": {
    "smart-code-reviewer": {
      "command": "npx",
      "args": [
        "-y",
        "https://github.com/renjismzy/mcp-code.git"
      ],
      "env": {
        "MCP_LOG_LEVEL": "info",
        "MCP_MAX_CODE_SIZE": "50000",
        "MCP_TIMEOUT": "30000"
      }
    }
  }
}
